# E-2-E ML model optimizer

The script loads an csv file and performs all the following operations:
* Data cleaning
* Feature Extraction
* Training Gradient Boosting, SVM and XGB model and optimize hyper parameters.